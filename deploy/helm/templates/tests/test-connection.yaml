apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "llm-proxy.fullname" . }}-test-connection"
  labels:
    {{- include "llm-proxy.labels" . | nindent 4 }}
    app.kubernetes.io/component: test
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "llm-proxy.fullname" . }}:{{ .Values.llm-proxy.service.port }}']
  restartPolicy: Never
